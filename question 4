#include <stdio.h>
int main()
float bs,gs;
printf("Enter basic salary");
scanf("%f",&bs);
if (bs <= 10,000)
gs=bs+bs*0.2+bs*0.8;
else if (bs=10,000 & bs <= 20,000) 
gs=bs+bs*0.25+bs*0.9;
else
gs= bs+bs*0.3+bs*0.95;
printf ("Gross salary Rs.%f",gs); 
return 0;
}
